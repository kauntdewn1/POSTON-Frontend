{
  "name": "poston-space",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install --include=dev && npm run build",
    "docker:dev": "docker-compose up --build",
    "start:api": "python app.py",
    "dev:api": "uvicorn app:app --host 0.0.0.0 --port 7860 --reload"
  },
  "dependencies": {
    "dotenv": "^16.6.1"
  }
}
